import { PreparedPricePipe } from './prepared-price.pipe';

describe('PreparedPricePipe', () => {
  it('create an instance', () => {
    const pipe = new PreparedPricePipe();
    expect(pipe).toBeTruthy();
  });
});
